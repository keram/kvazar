<div id="timer">	{$question->time}</div>{$question->form}<script type="text/javascript">//<![CDATA[			function countDown() {		var timer_elm = document.getElementById('timer');		if ( timer_elm ) {			var value = parseInt(timer_elm.innerHTML);			if ( value != 0  && !isNaN(value)) {				timer_elm.innerHTML = value - 1;			} else {				clearInterval(interval);			}		}				return 1;	}		interval = setInterval("countDown()", 1000);//]]></script>